/**
 * @type {import('next').NextConfig}
 *
 * Basic Next.js configuration enabling React strict mode. Additional
 * configuration options may be added as the project evolves.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
